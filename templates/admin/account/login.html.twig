<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="{{ asset('build/css/app.css') }}">
    <link rel="shortcut icon" href="https://symfony.com/favicons/favicon.ico">
    <title>Connexion à l'administration</title>
</head>
<body>
<div class="container mt-5">
    <div class="row">
        <div class="offset-md-3 col-md-6">
            <div class="card border-info p-3">
                <h1 class="card-header"><i class="fas fa-lock mr-3 "></i>Administration</h1>
                <div class="card-body">
                    <div class="alert alert-info alert-dismissible mb-4 px-3">
                        <button type="button" class="close pl-0" data-dismiss="alert">&times;</button>
                        <i class="fas fa-info-circle"></i>
                        Veuillez remplir ce formulaire pour vous connecter à l'administration
                    </div>
                    {% if hasError %}
                        <div class="alert alert-danger">Une erreur est survenue...</div>
                    {% endif %}
                    <form action="{{ path('admin_account_login') }}" method="post">
                        <div class="form-group">
                            <label for="email">Email</label>
                            <input type="text" name="_username" id="email" class="form-control"
                                   placeholder="Entrez votre email" value="{{ username }}" required>
                        </div>
                        <div class="form-group">
                            <label for="password">Mot de passe</label>
                            <input type="password" name="_password" id="password" class="form-control"
                                   placeholder="Entrez votre mot de passe" required>
                        </div>
                        <div class="form-group mb-0">
                            <button type="submit" class="btn btn-info">Se connecter</button>
                            <a href="{{ path('homepage') }}" class="btn btn-link d-block text-left text-info pl-0 mt-3">
                                <i class="fas fa-arrow-circle-left"></i>
                                Retour à l'accueil
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
</body>
</html>